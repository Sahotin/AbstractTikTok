services:
  analysis-api:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./database:/app/database
    restart: unless-stopped
